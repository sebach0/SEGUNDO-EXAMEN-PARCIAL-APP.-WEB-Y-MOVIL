# Scripts y SQL auxiliares

Los SQL numerados (`006_` … `009_`) en esta carpeta son históricos / manuales. El **init** de Postgres en Docker usa `backend/migrations/*.sql` según `docker-compose.yml`.

Para levantar contenedores y seeds, usá los comandos del **`README.md`** raíz (sección _Flujo habitual de comandos_).
#si
